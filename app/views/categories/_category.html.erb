<tr id="category-<%= category.id %>">
  <td><%= category.id %></td>
  <td><%= category.name.titleize %></td>
  <td><%= category.description.capitalize %></td>
  <td><%= category.products.count %></td>
  <td><%= link_to 'Ver', category_path(category) %> -
      <%= link_to 'Editar', edit_category_path(category) %> -
      <%= link_to 'Eliminar', category_path(category), method: :delete, data: { confirm: "Si elimina esta categoría, tambien eliminas #{category.products.count} productos. Estas seguro?" } %></td>
</tr>
