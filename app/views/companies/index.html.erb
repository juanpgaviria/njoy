<h1>Restaurantes</h1>
<% @companies.each do |company| %>
  <h2><%= link_to company.name, company_path(company) %></h2>
<% end %>
<%= link_to 'Salir', destroy_user_session_path, method: :delete %>
<%= link_to 'Crear nuevo restaurante', new_company_registration_path %>
