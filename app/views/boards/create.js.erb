<% if @board.errors.any? %>
  <% @board.errors.full_messages.each do |error| %>
    growl('danger', "<%= error %>", '#new_board')
  <% end %>
<% else %>
  $('.dropzone').append('<%= j render @board %>')
  $('#new-board').remove()
  $('.modal-backdrop').remove()
<% end %>
